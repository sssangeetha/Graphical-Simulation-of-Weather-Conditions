The Basic simulator works with a key-logger that enables the user to provide their input accordingly. Mouse-logger is also used to control the movements of objects in the scene. here, we created a scene consisting of several objects in a village on which different weather changes. These are shown based on the season selected through the input. This simulator enables the users to differenciate and analyze different seasonal changes accordingly.
